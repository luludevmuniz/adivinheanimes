query buscaInformacoesAnime($popularity_greater: Int!) {
    Media(popularity_greater: $popularity_greater) {
    id
    title{
        romaji
    }
    coverImage{
        extraLarge
    }
    }
}

query buscaMaiorPopularidade($id: Int!) {
    Media(id: $id) {
    popularity
    }
}